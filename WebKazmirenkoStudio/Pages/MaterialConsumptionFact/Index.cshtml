@page
@model WebKazmirenkoStudio.Pages.MaterialConsumptionFact.IndexModel
@using WebKazmirenkoStudio.Properties.Resources

@{
    ViewData["Title"] = ResourcesString.MaterialConsumptionFact;
    ViewData["CreateNewRecord"] = ElementPageResource.CreateNewRecord;
    ViewData["EditCaption"] = ElementPageResource.EditCaption;
    ViewData["DetailsCaption"] = ElementPageResource.DetailsCaption;
    ViewData["DeletePage"] = ElementPageResource.DeletePage;
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-page="Create">@ViewData["CreateNewRecord"]</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaterialConsumptionFactModel[0].MaterialConsumptionPlan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaterialConsumptionFactModel[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaterialConsumptionFactModel[0].Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.MaterialConsumptionFactModel) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MaterialConsumptionPlan.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Order.Id)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">@ViewData["EditCaption"]</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">@ViewData["DetailsCaption"]</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">@ViewData["DeletePage"]</a>
                </td>
            </tr>
        }
    </tbody>
</table>
